// constants must have explicit type annotation
// global scope :D
const MAX_COUNT: u8 = 10;


// static variables must have explicit type annotation
// accessing or modifying mutatable static is unsafe
static GAME_NAME: &str = "Draw Five";

fn main() {
    println!("Hello, world!");

    
}
